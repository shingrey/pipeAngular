import { ForNumberPipe } from './for-number.pipe';

describe('ForNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ForNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
